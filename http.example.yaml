# Test configuration with HTTP server
wait_interval: 30
secret_key: "test2310"
secret_salt: "SlTKeYOpHygTYkP3"

# HTTP server configuration for health checks
http_server:
  port: 2310

# HTTP configuration for testing
http_config:
  vault_urls:
    - "https://vault.example.com:8200"
  encrypted_keys: "WyJ0ZXN0Il0K" # base64 encoded ["test"]
